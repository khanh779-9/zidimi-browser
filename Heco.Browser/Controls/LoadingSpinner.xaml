<UserControl x:Class="Heco.Browser.Controls.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <Storyboard x:Key="Spin" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="Spinner"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                             From="0" To="360" Duration="0:0:0.9" />
        </Storyboard>
    </UserControl.Resources>
    <Grid Width="14" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Path x:Name="Spinner" Width="14" Height="14" Stretch="Uniform"
              Stroke="{DynamicResource HecoPurpleBrush}" StrokeThickness="2"
              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
              Data="M7 2 a5 5 0 1 1 -5 5"
              HorizontalAlignment="Center" VerticalAlignment="Center"
              RenderTransformOrigin="0.5,0.5">
            <Path.RenderTransform>
                <RotateTransform Angle="0" />
            </Path.RenderTransform>
            <Path.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource Spin}" />
                </EventTrigger>
            </Path.Triggers>
        </Path>
    </Grid>
</UserControl>
