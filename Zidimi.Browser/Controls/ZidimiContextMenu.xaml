<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Zidimi.Browser.Controls">

    
    <Style TargetType="{x:Type local:ZidimiContextMenu}">
        <Setter Property="MinWidth" Value="220" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ZidimiContextMenu}">
                    <Popup x:Name="PART_Popup"
                           Placement="{TemplateBinding Placement}"
                           PlacementTarget="{TemplateBinding PlacementTarget}"
                           HorizontalOffset="{TemplateBinding HorizontalOffset}"
                           VerticalOffset="{TemplateBinding VerticalOffset}"
                           StaysOpen="{TemplateBinding StaysOpen}"
                           AllowsTransparency="True"
                           PopupAnimation="Fade">
                        <Border BorderBrush="{DynamicResource StrokeBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                MinWidth="{TemplateBinding MinWidth}"
                                Margin="0,6,0,0"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="0" Opacity="0.55" />
                            </Border.Effect>
                            <Grid>
                                <Border x:Name="mask" Background="{DynamicResource ZidimiBgElevatedBrush}" CornerRadius="9" />
                                <ItemsControl ItemsSource="{Binding Items, RelativeSource={RelativeSource TemplatedParent}}"
                                              ItemTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                              Padding="4"
                                              SnapsToDevicePixels="True">
                                    <ItemsControl.OpacityMask>
                                        <VisualBrush Visual="{Binding ElementName=mask}" />
                                    </ItemsControl.OpacityMask>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </Popup>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
