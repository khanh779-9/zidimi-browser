<controls:ZidimiWindow x:Class="Zidimi.Browser.Views.ProfileSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Zidimi.Browser.Controls"
        xmlns:infra="clr-namespace:Zidimi.Browser.Infrastructure"
        mc:Ignorable="d"
        Title="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfilePicker_Title]}" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ZidimiBgSurfaceBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfilePicker_Title]}" 
                   FontSize="32" FontWeight="SemiBold" 
                   Foreground="{DynamicResource Ink100Brush}" 
                   HorizontalAlignment="Center" Margin="0,60,0,30" />

        <!-- Profile List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="ProfileItemsControl" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel MaxWidth="600" HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Background="Transparent" BorderThickness="0" Margin="10" Cursor="Hand"
                                Click="Profile_Click" Tag="{Binding}">
                            <Border x:Name="CardBorder" Width="140" Height="160" CornerRadius="12" Background="{DynamicResource ZidimiBgElevatedBrush}">
                                <Grid>
                                    <!-- Regular Profile -->
                                    <Grid Visibility="{Binding IsAddButton, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        
                                        <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5"
                                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                Click="MoreOptions_Click" Tag="{Binding}" Width="28" Height="28">
                                            <Button.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfileManager_Delete]}" 
                                                              Click="DeleteProfile_Click" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                            <!-- SVG for 3 vertical dots -->
                                            <Path Width="16" Height="16" Fill="{DynamicResource Ink200Brush}" Stretch="Uniform"
                                                  Data="M12,16A2,2 0 0,1 14,18A2,2 0 0,1 12,20A2,2 0 0,1 10,18A2,2 0 0,1 12,16M12,10A2,2 0 0,1 14,12A2,2 0 0,1 12,14A2,2 0 0,1 10,12A2,2 0 0,1 12,10M12,4A2,2 0 0,1 14,6A2,2 0 0,1 12,8A2,2 0 0,1 10,6A2,2 0 0,1 12,4Z" />
                                        </Button>

                                        <Ellipse Width="80" Height="80" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,15,0,0">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill" />
                                            </Ellipse.Fill>
                                        </Ellipse>

                                        <TextBlock Grid.Row="1" Text="{Binding Name}" 
                                                   FontSize="14" FontWeight="Medium" Foreground="{DynamicResource Ink100Brush}" 
                                                   HorizontalAlignment="Center" Margin="10,0,10,20" TextTrimming="CharacterEllipsis" />
                                    </Grid>

                                    <!-- Add Button -->
                                    <Grid Visibility="{Binding IsAddButton, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <Border Width="80" Height="80" CornerRadius="40" Background="{DynamicResource ZidimiBgSurfaceBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Path Width="30" Height="30" Stretch="Uniform" Fill="{DynamicResource Ink100Brush}"
                                                  Data="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <TextBlock Grid.Row="1" Text="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfilePicker_Add]}" 
                                                   FontSize="14" FontWeight="Medium" Foreground="{DynamicResource Ink100Brush}" 
                                                   HorizontalAlignment="Center" Margin="10,0,10,20" TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </Button>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="{DynamicResource CardHoverBrush}" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Bottom Panel -->
        <Grid Grid.Row="2" Margin="30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <controls:ZidimiButton Grid.Column="0" Content="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfilePicker_GuestMode]}" 
                                 Style="{DynamicResource ZidimiButtonSecondary}" Click="Guest_Click" />

            <controls:ZidimiCheckBox Grid.Column="2" x:Name="ShowOnStartupToggle" 
                                   Content="{Binding Source={x:Static infra:LanguageManager.Instance}, Path=[ProfilePicker_ShowOnStartup]}" 
                                   Click="ShowOnStartup_Toggled" HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</controls:ZidimiWindow>
